#pragma once

#include <obs-module.h>

/* The OBS source descriptor for the Eye Mask Tracker video filter.
 * Defined in mask-filter.cpp, registered in plugin-main.cpp. */
extern struct obs_source_info mask_filter_info;
